/** Feature flags — flip to re-enable without touching any other file. */

/** Set to true to re-enable payment gates (KnockoutGate, AI paywall, LS checkout). */
export const PAYMENTS_ENABLED = false;
